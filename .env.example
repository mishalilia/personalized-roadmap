# Copy to .env and fill in. Never commit the filled file.

# LLM provider API key (provider chosen in Q-003)
LLM_API_KEY=

# Model identifier
LLM_MODEL=

# Pinned version of the curriculum content we build against
CURRICULUM_VERSION=
